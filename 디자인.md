# 메모 앱 디자인 가이드 (노션 스타일)

Phase 5 메모 앱(`public/index.html`)에 적용하는 디자인 가이드. 메인 서비스(yumview)의 Toss 스타일과는 별개로, 이 데모는 노션(Notion) 특유의 차분하고 여백 넓은 문서형 UI를 따른다.

## 1. 컬러 팔레트

| 용도 | 색상 | 설명 |
|---|---|---|
| 배경 | `#ffffff` | 페이지 전체 배경 |
| 카드/입력 배경 | `#ffffff` | 메모 카드, 입력창 |
| 은은한 배경(hover) | `#f7f6f3` | 리스트 아이템 hover, 카드 hover |
| 기본 테두리 | `#e9e9e7` | 입력창, 카드 테두리 |
| 진한 텍스트 | `#37352f` | 제목, 본문 |
| 보조 텍스트 | `#787774` | 타임스탬프, placeholder |
| 옅은 텍스트 | `#9b9a97` | 빈 상태 안내문 |
| 포인트 색 | `#2383e2` | 버튼, 링크, 포커스 링 (노션 블루) |
| 위험/삭제 | `#eb5757` | 삭제 버튼 hover |

노션은 기본적으로 무채색 위주이고, 포인트 컬러 하나(블루)만 절제해서 사용한다. 채도 높은 색을 남발하지 않는다.

## 2. 타이포그래피

- 폰트: `-apple-system, BlinkMacSystemFont, "Segoe UI", "Malgun Gothic", Helvetica, Arial, sans-serif` (노션 실제 폰트 스택)
- 페이지 제목: 28px / 700 / `#37352f`
- 메모 본문: 16px / 400 / `#37352f` / line-height 1.6
- 타임스탬프: 12px / 400 / `#787774`
- 버튼 텍스트: 14px / 500

## 3. 레이아웃 & 간격

- 콘텐츠 최대 너비: 720px, 중앙 정렬, 좌우 패딩 24px
- 상단 여백: 80px (문서 첫 페이지 느낌)
- 카드 사이 간격: 8px (촘촘한 리스트, 카드끼리 거의 안 띄움 — 노션 블록 느낌)
- 카드 내부 패딩: 12px 14px
- 모서리 반경: 6px (버튼/입력창은 6px, 카드는 4px로 더 각지게)

## 4. 컴포넌트

### 입력창 (새 메모 작성)
- 테두리 없음 또는 `1px solid #e9e9e7`, 배경 `#ffffff`
- 포커스 시: 테두리 `#2383e2`, box-shadow `0 0 0 1px #2383e2`
- placeholder: "메모를 입력하세요..." / `#9b9a97`

### 추가 버튼
- 배경 `#2383e2`, 텍스트 흰색, hover 시 `#1a73cf`
- padding 8px 16px, radius 6px, font-weight 500

### 메모 카드 (리스트 아이템)
- 기본: 배경 흰색, 테두리 `#e9e9e7`
- hover: 배경 `#f7f6f3`, 우측에 수정/삭제 아이콘 버튼 나타남 (기본은 은은하게 숨겨져 있다가 hover 시 표시)
- 하단에 타임스탬프 (작성 시간, 상대 표시 또는 `YYYY.MM.DD HH:mm`)

### 수정 버튼
- 아이콘 버튼(연필 아이콘 또는 텍스트 "수정"), 회색 텍스트, hover 시 `#37352f`로 진해짐
- 클릭 시 카드가 인라인 편집 모드로 전환 (textarea + 저장/취소 버튼)

### 삭제 버튼
- 아이콘 버튼(휴지통 또는 텍스트 "삭제"), 기본 회색, hover 시 `#eb5757`

### 빈 상태
- "아직 작성한 메모가 없어요" / `#9b9a97` / 중앙 정렬, 상하 패딩 40px

## 5. 상호작용

- 모든 전환은 `transition: background-color 0.12s ease, border-color 0.12s ease` 정도로 짧고 절제되게 (노션 특유의 즉각적인 반응 속도)
- 그림자는 거의 쓰지 않는다 (평평한 문서 느낌). 꼭 필요하면 `0 1px 2px rgba(0,0,0,0.04)` 정도로 아주 약하게만.
- 버튼/카드에 큰 스케일 애니메이션(scale-up 등) 금지 — 노션은 절제된 인터랙션이 특징.
