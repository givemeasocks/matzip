# 맛집 리뷰 사이트 — 클로드 코드 작업지시서

> 이 문서는 실제 서비스 기획 문서가 아니라 **클로드 코드(Claude Code)에 Phase 순서대로 붙여넣어 진행할 실행 지시서**입니다.
> 각 Phase는 이전 Phase가 배포·검증된 것을 전제로 합니다. Phase를 건너뛰지 말고 순서대로 진행하세요.

---

## 공통 원칙 (모든 Phase에 적용)

- **Supabase는 반드시 Supabase MCP로 연결해서 작업할 것.** 로컬 키 복붙 없이 MCP를 통해 프로젝트 접속/테이블 생성/함수 생성을 진행한다.
- 배포는 Vercel 기준 (환경변수: 카카오 API 키, Google API 키, Gemini API 키, Supabase URL/anon key는 이미 등록되어 있음)
- 각 Phase 종료 시 Vercel에 배포하고 실제 동작 확인 후 다음 Phase로 넘어간다.
- 새 기능 추가 시 **기존 기능(검색/리뷰/AI분석/로그인)이 깨지지 않는지 회귀 확인**을 매 Phase마다 수행한다.

---

## Phase 0. 에이전트 팀 세션 #1 — 필터 고정 & 디자인(Toss 스타일)

> ✅ 디자인 방향은 **Toss 스타일로 통일**. Phase 1의 "design" 팀원도 Airbnb 대신 Toss 스타일을 적용하도록 수정했습니다.

**요청**: 에이전트 팀(Agent Teams)을 만들어 팀메이트 2명을 스폰한다. 서브에이전트가 아니라 **Agent Teams의 Teammate**로 스폰할 것.

- **팀원 1 "filter"**: 검색 필터 수정 — 검색 필터 없이 **서울로 고정**해서 검색되도록 변경
- **팀원 2 "design"**: 디자인 시스템을 **Toss 스타일**로 변경
- 두 팀원이 **동시에 같은 파일을 수정하지 않도록** 작업 범위를 분리해서 지시할 것

---

## Phase 1. 에이전트 팀 세션 #2 — LOGIC(담기/검색) & design(Airbnb 랜딩)

**요청**: 에이전트 팀을 만들어 팀메이트 2명을 스폰한다. (Agent Teams의 Teammate, 서브에이전트 아님)

### 팀원 1 "LOGIC"
- 맛집 담기 관련 로직 구현
- 키워드나 카테고리로 장소 검색 시 맛집 목록 출력
- **카카오 Local API** 사용
- 검색 결과는 **카드 형태**로 출력

### 팀원 2 "design"
- 랜딩 페이지 디자인 수정
- **Toss 디자인 시스템** 적용 (Phase 0의 방향과 통일)
- 현재 랜딩 페이지 디자인 수정 + 앞으로 구현될 "맛집 담기" 페이지 디자인도 함께 준비

### 공통 제약
- **두 팀원이 동시에 같은 파일을 수정하지 않도록** 작업 파일을 분리 지시 (예: LOGIC은 검색/API 로직 파일, design은 스타일/컴포넌트 파일)

---

## Phase 2. 구글 리뷰 보기 기능

> 전제조건: 카카오 API로 맛집 검색→목록 표시까지 완성되어 Vercel에 배포된 상태에서 시작.

### 2-1. 백엔드: 가게 이름 + 좌표로 구글 리뷰 조회

- **반드시 Google Places API의 최신 버전(New)을 사용**
- 입력: 가게 이름, 좌표(lat/lng)
- 검색 범위 제한: 넘겨받은 좌표에서 **도보 2분 거리(약 150m) 이내**에 있는 가게만 매칭 대상으로 함
  - Places API (New)의 Text Search 또는 Nearby Search에 `locationRestriction`(circle, radius=150)을 적용해 반경 내로 제한
  - fieldMask로 필요한 필드만 요청 (요금 절감): `id, displayName, rating, userRatingCount, reviews, googleMapsUri`
- 매칭 실패 시: "가게를 찾지 못했습니다" 응답을 명확히 반환 (에러가 아니라 정상 응답 형태로)
- 받아올 정보: 가게 이름, 별점, 리뷰 개수, 리뷰 내용, 구글맵 페이지 링크(googleMapsUri)
- 매칭된 결과는 **1개만**, 화면에서 바로 쓰기 좋은 정리된 JSON 형태로 반환

### 2-2. 프론트엔드: 리뷰 패널 UI

- 검색 결과 카드를 클릭하면 리뷰 영역이 열림 (아코디언/패널 형태)
- 리뷰 영역에 표시할 내용:
  - 가게 이름, 별점, 리뷰 개수
  - 리뷰 목록: 작성자, 별점, 작성 시점, 내용
  - "구글 맵에서 전체 리뷰 보기" 링크
- 로딩 중에는 **"리뷰를 불러오는 중.."** 텍스트 표시
- **브라우저 캐싱**: 한 번 조회한 가게는 브라우저(예: 메모리 상태 또는 sessionStorage 등)에 기억해두고, 같은 가게를 다시 클릭하면 API 재호출 없이 저장된 결과를 바로 보여줌
- 디자인은 **현재 사이트 디자인을 그대로 유지**

---

## Phase 3. AI 리뷰 분석 (Gemini)

> 전제조건: Phase 2까지 배포 완료, Gemini API 키 등록된 상태.

### 3-1. 분석 대상 3가지 (Gemini에게 요청)

1. 각 리뷰를 **긍정 / 보통 / 부정**으로 분류하고 개수 집계
2. 리뷰에 자주 등장하는 **핵심 단어 8~15개** 추출 (음식 이름, 맛, 분위기, 서비스 위주) — 각 단어가 **좋은 맥락인지 나쁜 맥락인지**도 함께 표시
3. 이 가게 리뷰 전체를 **한 문장으로 요약**

### 3-2. 동작 흐름

- 리뷰 데이터를 다 불러오면 **자동으로 이어서 AI 분석 시작**
- 분석 중에는 **"AI가 리뷰를 분석하는 중"** 표시
- **리뷰가 하나도 없는 가게는 분석 시도하지 않음** (분석 영역 자체를 숨김)

### 3-3. 결과 화면

1. **긍정/보통/부정 비율 막대** — 초록/노랑/빨강 색상
2. **워드클라우드** (널리 쓰이는 라이브러리 사용) — 핵심 단어들을 구름 형태로, 중요 단어일수록 크게, 좋은 맥락은 초록색·나쁜 맥락은 빨간색
3. **AI 총평 한 줄** — 말풍선 느낌으로 귀엽게 표시

### 3-4. 캐싱

- 같은 가게의 AI 분석 결과도 브라우저에 기억해두고, 재클릭 시 저장된 결과를 바로 보여줌 (재분석 안 함)

### 3-5. 디자인

- 지금 리뷰 패널과 어울리는 톤으로

---

## Phase 4. 로그인 기능 (Supabase Auth)

> Supabase는 MCP로 접속해서 프로젝트 생성/설정 진행.

### 4-1. UI

- 사이트 오른쪽 위에 **로그인 버튼** 추가
- 클릭 시 이메일/비밀번호 입력창 + **로그인 / 회원가입** 버튼이 있는 모달 표시
- 회원가입 완료 시 **이메일 인증 대기 없이 바로 로그인** 처리 (원래는 이메일 인증이 있어야 하지만 이번엔 생략)
- 에러 상황(비밀번호 오류, 이미 가입된 이메일 등)은 **한국어 안내 문구**로 표시

### 4-2. 로그인 상태

- 로그인 시 오른쪽 위에 **"{사용자이름}님 로그아웃"** 형태로 표시
- **새로고침해도 로그인 상태 유지** (세션 유지)
- 로그아웃 클릭 시 다시 "로그인" 버튼으로 복귀

### 4-3. 향후 확장 대비

- 비밀번호 처리는 **전부 Supabase Auth에 위임** (직접 해싱/검증 로직 만들지 않음)
- **로그인 여부와 무관하게 기존 기능(검색/리뷰/AI분석)은 계속 누구나 사용 가능**해야 함
- 나중에 "로그인한 사람만 담기 가능" 기능을 붙일 것이므로, **"현재 로그인한 사용자가 누구인지 확인하는 로직"을 다른 기능에서 재사용하기 쉬운 형태(공용 훅/유틸)로 분리**해둘 것

### 금지사항
- 비밀번호 직접 처리 금지 (Supabase에 위임)
- 비로그인 사용자의 기존 기능 접근 제한 금지 (담기만 로그인 필요)

---

## Phase 5. 메모 앱 (독립 데모, Supabase 실습용)

> 이 Phase는 메인 서비스와 별개의 **독립적인 index.html 데모**입니다.

### 요구사항
- Supabase에 메모 저장용 테이블을 직접 생성 (필드: 메모 내용, 작성 시간)
- 테이블 정책: **누구나 읽고 쓸 수 있게** (RLS 열어두거나 최소 제약)
- Supabase 프로젝트 URL/키는 **직접 찾아서 코드에 삽입** (사용자가 복붙하지 않아도 되게)
- 화면은 **index.html 파일 하나**로 구성:
  - 메모 작성 후 "추가" 버튼 → 저장
  - 저장된 메모는 **최신순**으로 표시
  - **수정** 버튼으로 내용 변경 가능
  - **삭제** 버튼으로 제거 가능
  - 디자인은 `디자인.md`에 **한국어로** 노션(Notion) 스타일 가이드를 작성한 뒤 그에 맞춰 제작
- 완성 후 **데모 테스트로 메모 3개를 직접 입력해 실제 저장되는지 확인**까지 수행

---

## Phase 6. 담기(북마크) 기능 + RLS

> 전제조건: 검색/리뷰/AI분석/로그인까지 완성된 상태. 가게 카드에 담기 버튼은 이미 있으나(모양만) 아직 기능 없음. Supabase에 저장 테이블 없음.

### 6-1. 테이블 설계 (SQL로 직접 실행 — 자동 생성 대신 SQL을 받아서 사용자가 Supabase에 직접 붙여넣어 실행)

저장할 항목:
- 누가 담았는지 (user_id)
- 가게 고유번호, 가게 이름, 카테고리, 주소, 좌표
- 담은 시간 (자동 기록)

```sql
create table saved_places (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  place_id text not null,
  place_name text not null,
  category text,
  address text,
  lat double precision,
  lng double precision,
  created_at timestamptz not null default now(),
  unique (user_id, place_id)  -- 같은 사람이 같은 가게 중복 담기 방지
);

alter table saved_places enable row level security;

create policy "select_own_saved_places"
  on saved_places for select
  using (auth.uid() = user_id);

create policy "insert_own_saved_places"
  on saved_places for insert
  with check (auth.uid() = user_id);

create policy "delete_own_saved_places"
  on saved_places for delete
  using (auth.uid() = user_id);
```

> `unique (user_id, place_id)` 제약으로 중복 담기를 DB 레벨에서 방지한다.

### 6-2. 프론트엔드 연결

- 담기 버튼 클릭 → 해당 가게 정보 1행 저장
- 이미 담긴 상태에서 다시 클릭 → 담기 취소 (해당 행 삭제)
- **이미 담아둔 가게는 검색 결과에 처음부터 "담김" 상태로 표시**
- 비로그인 상태에서 담기 클릭 → "로그인하면 담을 수 있어요" 안내 + 로그인 모달 오픈

### 금지사항
- 담기 기능 추가로 기존 기능(검색/리뷰/AI분석)의 동작이 바뀌면 안 됨

---

## Phase 7. 마이페이지 (담은 맛집 모아보기)

> 전제조건: 담기 완성, `saved_places`에 데이터 적재 확인됨. RLS로 본인 데이터만 조회 가능한 상태 확인됨.

### 요구사항
- `mypage.html` 신규 생성
- 메인 화면에 로그인 시 상단에 **"맛집 주머니"** 버튼 노출
- 마이페이지에서 담은 가게를 **카드**로 표시: 가게 이름, 주소, 카테고리, 담은 날짜, 구글맵 보기 링크
- **최근 담은 순으로 정렬**
- 카드의 **삭제(X)** 클릭 → 목록에서 제거 + `saved_places` 테이블에서도 삭제
- 아무것도 담지 않았을 때: **"아직 담은 맛집이 없어요, 검색하러 가볼까요?"** 안내 + 검색 페이지로 이동하는 버튼
- 디자인은 현재 사이트와 동일한 톤
- **데이터 조회 시 "내 것만" 조건을 코드에서 직접 걸지 않는다** — RLS가 알아서 본인 데이터만 반환하므로, 클라이언트는 그냥 전체 조회 쿼리를 보내면 됨

---

## Phase 8. 추천 기능 (인기 랭킹 + 맞춤 추천)

> 전제조건: 담기·마이페이지 완성. 이번 Phase 전에 `saved_places`에 **더미 데이터 100건**을 먼저 채워 넣고 진행.

### 8-1. 인기 랭킹 Top 5

- 메인 화면에 "지금 인기 맛집 Top 5" 코너
- 모든 사용자가 담은 것을 합산해 가장 많이 담긴 가게 순으로 5위까지 노출
- **RLS 때문에 일반 쿼리로는 본인 데이터만 보이므로**, 집계 전용 **Postgres 함수(SECURITY DEFINER)** 를 만들어 우회한다.
  - 이 함수는 **가게 이름과 담긴 횟수만** 반환하고, **누가 담았는지는 절대 노출하지 않음**

```sql
create or replace function get_popular_places(limit_count int default 5)
returns table (place_id text, place_name text, save_count bigint)
language sql
security definer
set search_path = public
as $$
  select place_id, place_name, count(*) as save_count
  from saved_places
  group by place_id, place_name
  order by save_count desc
  limit limit_count;
$$;

grant execute on function get_popular_places(int) to anon, authenticated;
```

> **주의**: 이 함수는 집계값만 반환하며 `user_id`를 select하지 않으므로 개인정보가 노출되지 않는다. `security definer`로 RLS를 우회하되, 함수 내부에서 특정 사용자 데이터가 아닌 집계만 다룬다는 점이 핵심이다.

### 8-2. 맞춤 추천

- 로그인 후 메인 홈에 "나를 위한 추천" 노출
- 로직: 내가 담은 가게들의 카테고리를 집계 → **가장 자주 담은 카테고리** 파악 → 그 카테고리의 다른 가게를 (카카오 API로) 검색해서 추천
- **이미 담은 가게는 추천 목록에서 제외**

### 금지사항
- **랭킹 구현 시 RLS를 끄지 말 것.** RLS는 유지한 채 Postgres 함수(SECURITY DEFINER)로 해결한다.

---

## 참고: Phase 진행 체크리스트

- [x] 디자인 방향: Toss 스타일로 통일
- [ ] Phase 2: Google Places API New, 150m 반경 제한 확인
- [ ] Phase 3: 리뷰 없는 가게 예외 처리 확인
- [ ] Phase 4: 새로고침 후 로그인 유지 확인
- [ ] Phase 6: SQL 직접 실행 → unique 제약 동작 확인
- [ ] Phase 8: RLS 유지 + SECURITY DEFINER 함수로 집계만 노출되는지 확인
